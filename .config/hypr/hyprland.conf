
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

# NVIDIA FIXES
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = WLR_NO_HARDWARE_CURSORS,1

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
monitor=HDMI-A-1, 1920x1080, 0x0, 1
monitor=DP-1, 1920x1080, 0x1080, 1
monitor=DVI-D-1, 1920x1080, 1920x1080, 1


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = waybar
exec-once = swww init
exec-once = dunst
# exec-once = $HOME/.local/share/hyprload/hyprload.sh
# exec-once = import-gsettings
exec-once = ~/.scripts/hypr-idle-dpms

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf
source = ~/.cache/wal/colors-hyprland.conf

# Some default env vars.
env = XCURSOR_SIZE,32

env = PATH,$HOME/.scripts:$PATH

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = caps:escape_shifted_capslock
    kb_rules =

    numlock_by_default = true

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 20
    border_size = 2
    col.active_border = $color2 $color1 90deg
    col.inactive_border = rgba(595959aa)

    col.group_border_active = $color1 $color2 90deg
    col.group_border = rgba(595959aa)

    cursor_inactive_timeout = 10

    # resize_on_border = true

    layout = master
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10

    blur {
        enabled = true
        size = 4
        passes = 1
        # special = false
    }

    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)

    dim_special = 0.0
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
    animation = specialWorkspace, 1, 6, default, slidevert
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
    # no_gaps_when_only = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

misc {
  enable_swallow = true
  swallow_regex = ^(Alacritty)$

  group_insert_after_current = false

  render_titles_in_groupbar = false
  groupbar_titles_font_size = 10
  groupbar_gradients = true

  no_direct_scanout = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

# workspace rules
workspace=name:focus,rounding:false,bordersize:0,gapsout:0,gapsin:0

# Plugins
plugin {
  split-monitor-workspaces {
    count = 5
  }
}


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, alacritty
bind = $mainMod, Q, killactive,
bind = $mainMod CONTROL ALT, Q, exit,
bind = $mainMod, N, exec, nemo
bind = $mainMod, W, exec, librewolf
bind = $mainMod, SPACE, togglefloating,
bind = $mainMod, D, exec, rofi -show drun
bind = $mainMod SHIFT, D, exec, rofi -show run
# bind = $mainMod, P, pseudo, # dwindle
# bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, F, fullscreen, 0
bind = $mainMod, M, exec, hyprctl keyword workspace $(hyprctl activewindow | grep "workspace: " | cut -d' ' -f2),gapsin:0,gapsout:0,bordersize:0,rounding:false,shadow:false
bind = $mainMod, R, exec, hyprctl reload
bind = $mainMod, GRAVE, togglespecialworkspace
bind = $mainMod SHIFT, GRAVE, movetoworkspace, special
# bind = $mainMod, F, togglespecialworkspace, focus
# bind = $mainMod, F, workspace, name:focus
# bind = $mainMod SHIFT, F, movetoworkspace, name:focus

# Group
bind = $mainMod, G, togglegroup,
bind = $mainMod SHIFT, G, moveoutofgroup,
bind = $mainMod CONTROL, G, exec, ~/.scripts/hypr-group-all

# Move into group with mainMod + vim keys
bind = $mainMod CONTROL, h, moveintogroup, l
bind = $mainMod CONTROL, l, moveintogroup, r
bind = $mainMod CONTROL, k, moveintogroup, u
bind = $mainMod CONTROL, j, moveintogroup, d

# Move focus with mainMod + vim keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move focus with mainMod + vim keys
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

# Switch workspaces with mainMod + [1-5]
# bind = $mainMod, 1, split-workspace, 1
# bind = $mainMod, 2, split-workspace, 2
# bind = $mainMod, 3, split-workspace, 3
# bind = $mainMod, 4, split-workspace, 4
# bind = $mainMod, 5, split-workspace, 5
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5

# Switch workspaces with mainMod
bind = $mainMod, PERIOD, focusmonitor, +1
bind = $mainMod, COMMA, focusmonitor, -1

# Move active window to a workspace with mainMod + SHIFT + [1-5]
# bind = $mainMod SHIFT, 1, split-movetoworkspacesilent, 1
# bind = $mainMod SHIFT, 2, split-movetoworkspacesilent, 2
# bind = $mainMod SHIFT, 3, split-movetoworkspacesilent, 3
# bind = $mainMod SHIFT, 4, split-movetoworkspacesilent, 4
# bind = $mainMod SHIFT, 5, split-movetoworkspacesilent, 5
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5

# Move active window to a workspace with mainMod + SHIFT
bind = $mainMod SHIFT, PERIOD, movewindow, mon:+1
bind = $mainMod SHIFT, COMMA, movewindow, mon:-1

# # Swap active workspaces with mainMod + CONTROL
# bind = $mainMod CONTROL, PERIOD, swapactiveworkspaces, +0 +1
# bind = $mainMod CONTROL, COMMA, swapactiveworkspaces, +0 -1

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Hyprload
# bind = SUPER SHIFT, R, hyprload, reload
# bind = SUPER SHIFT, U, hyprload, update
